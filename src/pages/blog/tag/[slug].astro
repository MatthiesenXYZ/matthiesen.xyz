---
import GhostPostsbyTag from "@components/blog/GhostPostsbyTag.astro";
const { slug } = Astro.params;
if(slug === undefined){ throw new Error('Slug is required'); }
---
<GhostPostsbyTag slug={slug} />