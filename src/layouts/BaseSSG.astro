---
import Base from "./BaseSSR.astro";

interface Props { 
  title: string
  isStorePage?:boolean
}

const { title, isStorePage } = Astro.props;

export const prerender = true;
---
<Base title={title} isStorePage={isStorePage}>
  <slot />
</Base>
